<Window x:Class="ExpansesManager.MainApp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainApp" Height="400"
        WindowStartupLocation="CenterScreen"
        Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="1" Grid.Column="0">
            <Label Content="Groups: " />
            <DataGrid x:Name="GroupsGrid"
                      AutoGenerateColumns="False"
                      SelectionUnit="FullRow"
                      CanUserAddRows="True"
                      SelectedIndex="0"
                      CanUserDeleteRows="True">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Is Active" Binding="{Binding Path=IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></DataGridCheckBoxColumn>
                    <DataGridTextColumn Header="Group Name" Width="*" Binding="{Binding Path=Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <Grid Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Vertical" Grid.Row="0">
                <Label Content="Sub Groups: " />
                <DataGrid x:Name="SubGroupsGrid"
                          DataContext="{Binding GroupsGrid.SelectedItem}"
                          ItemsSource="{Binding Path=SubGroups}"
                      AutoGenerateColumns="False"
                      SelectionUnit="FullRow"
                      CanUserAddRows="True"
                      SelectedIndex="0"
                      CanUserDeleteRows="True">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Is Active" Binding="{Binding Path=IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></DataGridCheckBoxColumn>
                        <DataGridTextColumn Header="Group Name" Binding="{Binding Path=Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
            <StackPanel Orientation="Vertical" Grid.Row="1">
                <Label Content="Elements: " />
                <DataGrid x:Name="ElementsGrid"
                          DataContext="{Binding SubGroupsGrid.SelectedItem}"
                          ItemsSource="{Binding Path=Elemetns}"
                      AutoGenerateColumns="True"
                      SelectionUnit="FullRow"
                      CanUserAddRows="True"
                      SelectedIndex="0"
                      CanUserDeleteRows="True">
                    <!--<DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Is Active" Binding="{Binding Path=IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></DataGridCheckBoxColumn>
                        <DataGridTextColumn Header="Group Name" Binding="{Binding Path=Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                    </DataGrid.Columns>-->
                </DataGrid>
            </StackPanel>
        </Grid>
        <Button x:Name="LogoutButton" Content="Logout" Grid.Column="1" HorizontalAlignment="Left" Margin="212.2,10,0,0" VerticalAlignment="Top" Width="75" Grid.RowSpan="2" Click="LogoutButton_Click"/>
    </Grid>
</Window>